SPECKLE_TOKEN="mytoken"
SPECKLE_SERVER_URL="http://127.0.0.1:3000"
SPECKLE_PROJECT_ID=""
SPECKLE_AUTOMATION_ID=""
# Optional. Property key the integration test writes conditioning output
# under. Unset, the test writes to the same key the deployed function uses
# ("Conditioned UF Code"), which will overwrite a live conditioned model's
# own property — set this to something disposable when exercising the live
# path against a project someone is actually reading.
# CONDITIONING_CODE_PROPERTY_NAME="Conditioned UF Code (test)"
